# Remaining Surfaces Audit Report — Aranya Ceylon — 2026-09-03

Scope: the customer-facing surfaces not covered by the two prior audit cycles —
product catalog + search, public blog/journal, account/wishlist/order-tracking,
and gift sets + recipes (including their admin CRUD, which had never been
reviewed — the earlier admin-console audit only covered orders/products/blog/
dashboard). Four parallel focused passes, each independently verified against
the actual code (not survey-level).

Status legend: `pending` | `in-progress` | `done` | `skipped (reason)`

**Headline:** no auth/IDOR/payment-integrity holes were found on any of the
four surfaces — the ownership-check discipline from the earlier audits (filter
by `userId` in the query itself, not after fetch) holds up consistently in
wishlist, orders, and addresses. What this pass found instead is a different
class of problem: **real backend features whose frontend never actually
consumes them**, and **two features that were scaffolded but never fully wired
end-to-end** (gift sets, recipe shopping) — both of which mean money can
silently not reach the cart/checkout it visually appears to reach. Two
findings (#2, #3) are genuinely serious: admin-edited gift-set prices don't
affect what customers are charged, and admin-created gift sets are never
actually purchasable.

## Wave 1 — High

| # | Area | File:line | Finding | Fix approach | Status |
|---|---|---|---|---|---|
| 1 | Product | `aranya-next/src/lib/spice-data.ts:82-94` (`headlinePrice`) | For a product with variants in only ONE market (no `BOTH`), viewing it under the *other* market falls back to `pool = p.variants` (any currency) but still formats with the target market's symbol — e.g. an LKR-only product viewed under `INTERNATIONAL` prints `"$2150.00"` for a value that's actually Rs 2150 (~150x mislabeled). Feeds JSON-LD `offers.price`, card price fallback, and cart base-price fallback. | When no matching-currency variant exists, don't silently reuse a wrong-currency price — surface "not available in your region" (matching how `product.controller.ts`'s market-scoped variant filtering already excludes it server-side) instead of mislabeling the number. | pending |
| 2 | Gifts | `backend/src/controllers/admin/gift.admin.controller.ts:72-102` (`updateGift`) | `GiftSet.usd`/`lkr` (display price, edited in `AdminGifts.tsx`) is entirely disconnected from the backing `Product`'s `Variant.price`, which is what checkout actually charges. Admin lowers/raises a gift set's price → `/gifts` shows the new number instantly, but checkout still charges the old `Variant.price`. Verified by reading `updateGift` (only writes `GiftSet`) against `cart.service.ts`/checkout (reads `Variant.price`) — confirmed disconnected. | Either make `Variant.price` the single source of truth (drop the separate `GiftSet.usd`/`lkr` display fields, derive display price from the variant) or have `updateGift` write through to the backing variant's price atomically when one exists. | pending |
| 3 | Gifts | `backend/src/controllers/admin/gift.admin.controller.ts:46-70` (`createGift`) | `createGift` only inserts a `GiftSet` row — never creates the backing `Product`/`Variant` that `gift.controller.ts`'s own comment says is required for the gift to be "purchasable." A gift set created through the admin UI publishes fine, appears on `/gifts`, "Add to basket" shows a success toast — but `CartContext.tsx` only syncs to the server cart `if (line.productId && line.variantId)`, both of which are null. It silently vanishes from the real order at checkout. Verified: `gift.controller.ts:9-10` explicitly documents "Backing product missing (not yet seeded) → productId null, and the gift simply isn't purchasable yet (graceful)" — but "graceful" here means silent, not blocked. | `createGift` should either create the backing `Product`+`Variant` in the same transaction, or the admin UI must refuse to publish (or clearly warn) a gift set with no backing product until one exists — never let it reach "PUBLISHED" in a silently-unpurchasable state. | pending |
| 4 | Recipes | `aranya-next/src/lib/recipes-data.ts:199-200` (`recipeSpices`) | Recipe "shop the spices" / "add all to basket" resolves ingredient names against the **hardcoded static demo catalog**, unconditionally — not the live backend catalog, regardless of whether the API is reachable. Every add-to-cart from a recipe page becomes a local-only cart line (same `productId`/`variantId`-null mechanism as #3) with a stale demo price. This is the only code path the feature has — not an edge case. | Resolve recipe spice names against the live product catalog (by slug/id stored on the recipe, not a free-text name match against demo data), so `cart.add()` receives a real `productId`/`variantId` and the line actually reaches checkout. | pending |

## Wave 2 — Medium

| # | Area | File:line | Finding | Fix approach | Status |
|---|---|---|---|---|---|
| 5 | Account | `aranya-next/src/lib/account-data.ts:303` (`toAccountOrder`) | `shipTo` is hardcoded to `""` for every real (non-demo) order, even though the backend already returns `order.shippingAddress` and the frontend `Order` type declares the field. Result: the "Shipping to" card on every real order's tracking page (`AccountTracking.tsx:254`) never renders — the address exists in the API response and is simply discarded before display. | Read `order.shippingAddress` in `toAccountOrder()` instead of hardcoding `""`. | pending |
| 6 | Blog | `backend/src/services/blog.service.ts:3-8` | `req.query.cursor` is passed straight into Prisma's `cursor: { id: cursor }` with no existence check. A nonexistent or stale (since-deleted-post) cursor makes Prisma throw, 500ing the public, unauthenticated blog listing endpoint. | Validate the cursor exists before using it (or catch the specific Prisma not-found error and treat it as "no more results" / empty page). | pending |
| 7 | Blog | `aranya-next/src/lib/journal-data.ts:136`, `JournalClient.tsx:87-106` | The category filter chips are hardcoded to 3 fixed values (`Sourcing`/`Spice Notes`/`Heritage`) matched by exact string against a live post's first tag. Any real post tagged anything else (or untagged, defaulting to `"Journal"`) is visible under "All" but permanently invisible under every specific filter chip, with no explanation to the reader. | Either derive the filter chip set dynamically from the tags actually present in live posts, or map arbitrary tags into the fixed category set explicitly (with a documented convention for admins) rather than requiring an exact-string match. | pending |
| 8 | Blog | `aranya-next/src/app/journal/[slug]/page.tsx:23` | "Related articles" / "Keep reading" always reads the static demo `JOURNAL` array, never the live backend, regardless of the current article's source. Every real published article shows the same 3 fixed, topically-unrelated demo posts as "related." | Query real related posts from the live blog API (e.g. by shared tag, or "recent excluding current") instead of the static demo array. | pending |
| 9 | Product | `CatalogClient.tsx:107-108`, `SearchClient.tsx:156` | "Price: Low to High/High to Low" sorts by `p.usd` unconditionally, never `p.lkr` — a LOCAL-market shopper's price sort is ordered by a currency they aren't even seeing, which need not correlate with the LKR prices actually displayed. | Sort by the price field matching the active market, mirroring how display price already resolves per-market. | pending |
| 10 | Product | `aranya-next/src/lib/catalog-data.ts:86-97` (`toCatalogSpice`) | `flavour` is hardcoded to `[]` for every live (API-backed) product — there's no such field on the backend `Product`. Every flavour-filter deep-link (`/products?flavour=Sweet`, linked from `CategoriesClient.tsx`) permanently shows "Nothing matches those filters" once real data replaces the demo fallback; the Flavour dropdown itself just silently disappears rather than surfacing as broken. | Either add a real `flavour`/tag field to `Product` and populate it, or remove the flavour filter UI and deep-links until backend support exists — the current state is a dead, silently-broken feature for every real product. | pending |
| 11 | Gifts | Architectural — `checkout.controller.ts`, `webhook.controller.ts`, `seed-gifts.ts:109,148-168` | Gift-box sales decrement only the gift box's own synthetic `Variant` stock (hand-set to 999 in the seed script), completely independent of the real stock of the component products listed in `GiftSet.contents`. A component product can sell out on its own product page while the gift box containing it keeps selling indefinitely. | Decide the intended model: either reserve/decrement each component's real stock when a gift box sells, or explicitly document gift boxes as stock-unlimited-by-design and set expectations accordingly. Needs a product decision, not just a code fix. | pending |
| 12 | Gifts | `aranya-next/src/components/admin/AdminRecipes.tsx:122-131` (`handleSave`) | The recipe editor's save path deliberately omits `spices`/`ingredients`/`method`/`tips`/`intro` — only title/slug/course/difficulty/times/serves/featured/status are editable. A recipe created via "New recipe" can be published with zero actual content, and there is no UI path to add it, even though the backend fully supports it. | Extend the admin recipe editor to expose the content fields the backend/schema already support. | pending |

## Wave 3 — Low

| # | Area | File:line | Finding | Fix approach | Status |
|---|---|---|---|---|---|
| 13 | Account | `AccountDashboard.tsx:141,150-151,57` | Dashboard "Harvest points" (1840/2000), tier ("Gold"), "Member since," and saved-spices count are read from the static demo `ACCOUNT.user` object unconditionally — every real signed-in user sees identical fake values regardless of actual account state. (No loyalty-points backend feature appears to exist at all.) | Either wire these to real backend fields (if/when a loyalty feature ships) or remove the fabricated stats from the real-user view. | done |
| 14 | Account | `AccountDashboard.tsx:282-309` (`ProfileField`, `saveProfile`) | The Profile "Phone" field shows a hardcoded fake value and is an uncontrolled input; `saveProfile` only sends `{ name }` to the backend (no phone field exists in `patchMeSchema` either). Editing phone and clicking "Save changes" silently discards the edit with no error shown. | Either add a real `User.phone` column + schema field end-to-end, or remove the phone field until it's backed by something. | done |
| 15 | Account | `AccountDashboard.tsx:340-342` | "The Harvest List" newsletter toggle is a styled `<span>` with no `onClick`/state/API call — always shows "on," can't be changed. | Wire to a real preference field, or remove the control if no such preference exists yet. | done |
| 16 | Account | `AccountDashboard.tsx:192-199,212-216` | If `getAddresses()` fails, the UI falls back to a fabricated 3-row demo address list with synthetic ids (`"0"`,`"1"`,`"2"`). "Remove" on one of these fires a real (harmless, 404) `DELETE` but optimistically removes the row from local state regardless of whether the backend call actually succeeded (errors silently swallowed) — a transient network blip produces an address list that looks editable but doesn't reflect real state. | On address-fetch failure, show an explicit error/retry state instead of a fabricated editable list; don't optimistically mutate state ahead of a call whose failure is silently swallowed. | done |
| 17 | Account | `AccountTracking.tsx:214-217,266-270` | "Invoice" and "Report an issue" buttons have no `onClick` — same dead-control pattern flagged repeatedly in the earlier admin audit. | Wire to real functionality or remove until built. | done |
| 18 | Blog | `ArticleClient.tsx:103-112` | Instagram/Facebook/Pinterest "share" icons are bare `<span>`s with no `href`/`onClick`/`aria-label` — non-functional decoration. | Wire to real share intents (or `navigator.share`) or remove. | done |
| 19 | Blog | `aranya-next/src/app/journal/page.tsx:17`, `JournalClient.tsx` | Journal index fetches a fixed `limit: 30` once; the API's `nextCursor` is discarded, so posts beyond the 31st are unreachable from `/journal` (still reachable by direct slug and via `/search`, which fetches `limit: 50` independently). | Wire pagination/load-more using the already-returned `nextCursor`. | done |
| 20 | Product | `MarketContext.tsx:34-49` (`setMarket`) | Optimistic client-side market flip happens before `overrideMarket()`'s server round-trip completes and `router.refresh()` re-fetches — a brief window where price components render the new market's label against data still fetched under the old market cookie (wrong-currency flash). | Consider deferring the visible market label flip until `router.refresh()` resolves, or accept as a known latency-bound cosmetic gap. | done |
| 21 | Product | `shared/src/schemas/product.schema.ts:73` | `search` query param has no `.max()` length cap, unlike every other string field in the schema; feeds `plainto_tsquery`/`similarity()` on every request. Request-count rate limiting doesn't bound per-request cost. | Add a `.max()` cap consistent with the rest of the schema. | done |
| 22 | Product | `backend/src/routes/product.routes.ts:10-13` | Fixed routes (`/featured`, `/bestsellers`, `/search`) are registered before the catch-all `/:slug`, so a product whose slug happens to exactly match one of those three words is permanently unreachable via its own product page. | Namespace the fixed routes (e.g. under a prefix) the way the admin routes already are, or reorder/guard the catch-all. | done |

## Wave 3 fix notes

- **#13:** Removed the fabricated loyalty block (tier/points progress bar, "Member since") from `Sidebar` and the "Harvest points" stat tile from `OverviewView`, per explicit user decision. The "Saved spices" stat tile now reads a real wishlist count — the wishlist fetch was lifted from `WishlistView` up into `AccountDashboard` so the overview tile and the Wishlist tab share one fetch instead of the tile reading fake demo data while the real count was fetched separately and correctly elsewhere.
- **#14 / #15:** Per explicit user decision, built real backend support rather than removing the controls: added `User.phone` (`String?`) and `User.newsletterOptIn` (`Boolean @default(true)`) to the schema (migration `20260903120000_add_user_phone_and_newsletter`), extended `patchMeSchema`/`patchMe`/`getMe`/`login`/`refresh` to carry both fields, and wired `ProfileView`'s phone field (now controlled) and the newsletter toggle (now a real optimistic-with-revert button) to `PATCH /auth/me`. **Needs `prisma migrate deploy` before merging** — this worktree has no DB access to run it.
- **#16:** `AddressesView` no longer falls back to a fabricated demo address list on fetch failure — shows an explicit "Couldn't load your saved addresses — Try again" state instead, matching the loading/empty-state pattern already used elsewhere in the same view.
- **#17:** Removed both dead buttons (no backend capability exists for either — same reasoning as the "Packing slip" removal in the admin console's Wave 3).
- **#18:** Wired real share intents: Facebook/Pinterest open their standard web share-intent popups; Instagram has no public web share-intent, so it copies the link and shows "Link copied — share on Instagram" (same fallback pattern real sites use for this exact gap).
- **#19:** `JournalClient` now fetches the next page via the already-returned `nextCursor` and appends to the loaded set, with a "Load more stories" button styled to match `CatalogClient`'s existing load-more control.
- **#20:** `MarketContext.setMarket` now flips the visible market label only once `overrideMarket()` confirms the cookie (or on failure, still flips so the switcher isn't stuck offline) — shrinks the mismatched-currency window from the full round-trip to near-zero.
- **#21:** Added `.max(200)` to `productFilterSchema.search`, consistent with every other string field in the schema. Confirmed this schema is actually applied (`productFilterSchema.parse(req.query)` in `product.controller.ts`'s `listProducts`), not dead validation.
- **#22:** Rather than reordering/renaming the existing public routes (`/products/featured` etc. are live API paths the frontend already calls), added a reserved-slug check to `createProductSchema`/`updateProductSchema` so a product can never be assigned a slug that collides with a fixed route segment going forward. Does not retroactively rename an already-existing colliding product, if one exists.
- **Verification:** cannot run `npx tsc --noEmit` / `npx vitest run` / `prisma migrate deploy` from this worktree (no `node_modules`, no DB access). See the recipe below.

## Wave 4 — Informational (no fix required, or needs a product decision first)

| # | Area | File:line | Finding | Status |
|---|---|---|---|---|
| 23 | Account | `order.controller.ts:20-24`, `wishlist.controller.ts:7-24` | No pagination on the account order-list/wishlist-list endpoints — strictly `userId`-scoped (not a security issue), bounded by the global rate limiter, but unbounded per-request payload for a user with a very large history. Worth a limit+cursor at scale, not urgent. | skipped (not urgent — no security impact, scale concern only) |
| 24 | Gifts | `giftSchema`/`recipeSchema` (admin controllers) | No `.max()` bounds on string/array fields (`contents`, `spices`, `method`, `tips`, `ingredients[].items`) — matches the pre-existing uncapped pattern already present in `blog.admin.controller.ts`, not a gap newly introduced on this surface. | skipped (pre-existing pattern elsewhere, not a regression on this surface) |

## What was checked and found clean (not regressions to introduce)

- **IDOR**: every account-scoped endpoint (wishlist, orders, addresses) filters by `userId` in the Prisma query itself; the shared guest/authenticated `/orders/:id` route correctly branches and 403s cross-user access.
- **Admin gift/recipe RBAC**: `requireAuth` + `requireRole('ADMIN','SUPERADMIN')` + `adminLimiter` correctly inherited by all 10 gift/recipe admin endpoints; every admin update/delete does an ownership/existence `findUnique` before mutating.
- **Client-supplied price injection**: `addToCartSchema` accepts only `productId`/`variantId`/`quantity` — no client-controlled price field anywhere in cart/checkout, gift sets included.
- **Blog visibility**: public listing/detail endpoints correctly filter `status: PUBLISHED`; no way to fetch a draft/scheduled post by slug or id.
- **XSS/sanitization**: rich-text article bodies sanitized on every render path; JSON-LD and Next Metadata both properly escape article-derived strings.
- **SQL injection**: both raw product-search queries use parameterized tagged templates.
- **Pagination bypass / ReDoS**: product listing capped via Zod; search-token regex escapes metacharacters first.
- **Public recipes page**: does call the live backend with demo-data fallback only when unreachable (contrary to a stale README note) — admin-published recipes do reach real customers on `/recipes`.

## Verification state

Each of the four sub-audits was run by an independent focused pass reading the actual code end-to-end (not a survey), and two of the highest-severity findings (#2, #3) were independently re-verified by direct read before writing this report. Nothing has been fixed yet — this is audit-only. `npx vitest run` / `npx tsc --noEmit` have not been run against any change because no change has been made.
